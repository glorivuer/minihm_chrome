# MiniHermes Core Dependencies
# Only includes packages required for the local CLI, Telegram Gateway, and AIAgent core.

# LLM Inference
openai==2.24.0
pydantic==2.13.4

# Networking & HTTP
httpx[socks]==0.28.1
requests==2.33.0
python-dotenv==1.2.2

# Telegram Gateway Adapter
python-telegram-bot[webhooks]==22.6

# Local CLI & TUI Output
prompt_toolkit==3.0.52
rich==14.3.3
Markdown==3.10.2

# Configuration & Utilities
pyyaml==6.0.3
ruamel.yaml==0.18.17
tenacity==9.1.4
jinja2==3.1.6
croniter==6.0.0
psutil==7.2.2
pathspec==1.1.1
fire==0.7.1

# Webhooks & Async Server (required for Telegram Webhook mode)
fastapi>=0.104.0,<1
uvicorn[standard]>=0.24.0,<1
ptyprocess>=0.7.0,<1; sys_platform != 'win32'
